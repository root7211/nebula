-- =============================================================================
-- wgpu_bindings.nelua
-- wgpu-native v29.0.0.0 的 Nelua C 绑定（Facade）
--
-- ★ P1-3 refactor: 原 1121 行 / 48 处条件编译 → 3 文件拆分 + 1 行条件分发。
--   · wgpu_bindings_shared.nelua  — 平台无关定义（handles, 共享枚举/结构体/函数）
--   · wgpu_bindings_native.nelua  — Native 专属（wgpu-native v29 布局）
--   · wgpu_bindings_wasm.nelua    — WASM 专属（Emscripten 布局）
-- =============================================================================

## if NEBULA_TARGET ~= 'wasm' then
require "wgpu_bindings_native"
## else
require "wgpu_bindings_wasm"
## end
